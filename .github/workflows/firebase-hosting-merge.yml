# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy playreport frontend to Firebase Hosting on merge
on:
  push:
    branches:
      - develop
defaults:
  run:
    working-directory: playreport-concater-frontend
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "checkout"
        uses: actions/checkout@v4
      - name: "setup bun"
        uses: oven-sh/setup-bun@v2
      - name: "bun  install"
        run: bun install
      - name: "build"
        run: bun run build
      - run: ls
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: "."
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SCHOOL_IDOL_STAGE_SUPPORTER }}
          channelId: live
          projectId: school-idol-stage-supporter